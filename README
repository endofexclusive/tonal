# tonal - tonal music library

## Instructions
See `include/tonal.h` for API.

Compile like this:

    $ cc -Iinclude -std=c99 -Wall -Wextra -pedantic -c tonal.c -o tonal.o

## Unit tests

Unit tests are provided in the `test` subdirectory. They depend on the `vtest`
unit test framework referenced as a GIT submodule in `test/vtest`.

